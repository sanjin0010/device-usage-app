\# 设备使用登记与预约APP —— 产品需求文档 (PRD)



> 文档版本：v1.0  

> 创建日期：2026-08-13  

> 目标平台：移动端（iOS / Android）  

> 开发工具：Codex (AI辅助开发)



\---



\## 1. 项目背景与目标



\### 1.1 背景

当前厂房内设备使用管理依赖纸质登记或口头沟通，存在信息不透明、时间冲突频发、记录难以追溯等问题，严重影响设备利用效率和管理成本。



\### 1.2 目标

开发一款移动端APP，实现：

\- 设备信息的数字化管理，实时展示每台设备的状态（空闲/使用中/已预约）。

\- 员工可快速登记设备使用（开始/结束时间），操作简便。

\- 支持未来时间段的预约，系统自动检测时间冲突并阻止重复预约。

\- 管理员可查看所有使用记录和统计数据，辅助决策。



\---



\## 2. 用户角色



| 角色 | 职责与权限 |

|------|------------|

| \*\*普通员工\*\* | 查看设备列表及详情；扫描设备二维码；登记使用（开始/结束）；预约设备；查看自己的使用记录和预约记录；取消自己的预约（未开始前）。 |

| \*\*设备管理员\*\* | 所有普通员工权限；新增/编辑/删除设备信息；查看所有设备的使用历史和预约记录；导出统计报表（可选）。 |



\---



\## 3. 功能需求



\### 3.1 设备信息管理（基础模块）

\- 设备台账应包含字段：

&#x20; - 设备名称（必填）

&#x20; - 设备型号（选填）

&#x20; - 设备编号（唯一，可自动生成）

&#x20; - 所在厂房/位置（选填）

&#x20; - 设备照片（可上传多张）

&#x20; - 设备状态（由系统自动维护：空闲/使用中/已预约）

&#x20; - 二维码（系统自动生成，用于快速扫码操作）

\- 管理员功能：增、删、改设备信息；可批量导入/导出（CSV或Excel）。



\### 3.2 设备使用登记（核心功能）

\- \*\*快速选择设备\*\*：

&#x20; - 通过设备列表、搜索框、或扫描设备上的二维码进入设备详情页。

\- \*\*登记使用\*\*：

&#x20; - 在设备详情页，点击“登记使用”按钮。

&#x20; - 系统自动填充当前时间为“开始时间”（允许手动修改）。

&#x20; - 员工需填写“结束时间”（预计结束时间，可手动修改）。

&#x20; - 可选填写“使用事由/备注”。

&#x20; - 提交后，系统记录本次使用，设备状态变为“使用中”。

\- \*\*结束使用\*\*：

&#x20; - 使用完毕后，员工在设备详情页或“我的使用中”列表点击“结束使用”，系统记录实际结束时间，设备状态恢复“空闲”。



\### 3.3 设备预约管理（进阶功能）

\- \*\*查看可用时间\*\*：

&#x20; - 在设备详情页，以\*\*日历视图\*\*或\*\*时间线\*\*展示已被占用的时间段（包括正在进行的使用和已被预约的未来时段），帮助员工找到空闲窗口。

\- \*\*发起预约\*\*：

&#x20; - 点击“预约”按钮，选择未来的开始时间和结束时间。

&#x20; - 填写预约事由。

&#x20; - 系统\*\*自动检测时间冲突\*\*：若所选时间段与任何已有使用或预约冲突，则弹出提示并禁止提交；若无冲突，则保存预约，设备状态变为“已预约”。

\- \*\*我的预约\*\*：

&#x20; - 在个人中心可查看所有预约记录（待使用、使用中、已结束、已取消）。

&#x20; - 对于“待使用”状态的预约，员工可在预约开始前\*\*取消预约\*\*，取消后设备释放该时间段。



\### 3.4 记录与统计（管理模块）

\- \*\*使用记录\*\*：所有使用和预约均生成历史记录，包含设备、员工、时间、事由等，可按设备、时间、员工等条件筛选。

\- \*\*统计看板（管理员）\*\*：

&#x20; - 显示每台设备的使用总时长、使用次数、空闲率。

&#x20; - 可生成报表（如月度设备使用报告），用于设备采购和维护决策。



\### 3.5 通知与提醒（可选）

\- 在预约开始前15分钟，向预约人发送推送通知或应用内提醒。

\- 若设备使用超时（超过预计结束时间），可向当前使用者发送提醒。



\---



\## 4. 数据模型（核心实体）



\### 4.1 设备表 (Devices)

| 字段名 | 类型 | 说明 |

|--------|------|------|

| id | UUID | 主键 |

| name | String | 设备名称 |

| model | String | 型号 |

| code | String | 唯一编号 |

| location | String | 位置 |

| photos | \[String] | 图片URL数组 |

| status | Enum | 空闲/使用中/已预约 |

| createdAt | Datetime | 创建时间 |

| updatedAt | Datetime | 更新时间 |



\### 4.2 使用记录表 (UsageRecords)

| 字段名 | 类型 | 说明 |

|--------|------|------|

| id | UUID | 主键 |

| deviceId | UUID | 关联设备 |

| userId | UUID | 关联使用者 |

| startTime | Datetime | 实际开始时间 |

| endTime | Datetime | 实际结束时间（可为空） |

| plannedEndTime | Datetime | 计划结束时间（登记时填写） |

| note | String | 备注 |

| type | Enum | 使用/预约 |

| status | Enum | 进行中/已结束/已取消 |

| createdAt | Datetime | 创建时间 |



\### 4.3 预约记录表 (Reservations) （可合并到使用记录表中，通过type区分）

若独立则包含：

| 字段名 | 类型 | 说明 |

|--------|------|------|

| id | UUID | 主键 |

| deviceId | UUID | 关联设备 |

| userId | UUID | 预约人 |

| startTime | Datetime | 预约开始 |

| endTime | Datetime | 预约结束 |

| reason | String | 预约事由 |

| status | Enum | 待使用/已使用/已取消 |

| createdAt | Datetime | 创建时间 |



\---



\## 5. 界面草图（简要描述）



\- \*\*首页\*\*：顶部搜索框，下方为设备列表，每个设备卡片显示名称、位置、当前状态（绿/红/黄圆点），点击进入详情。

\- \*\*设备详情页\*\*：顶部设备照片轮播，下方基本信息，接着是\*\*时间轴/日历\*\*展示占用情况，底部两个大按钮：“登记使用”和“预约”。

\- \*\*登记/预约表单\*\*：弹出或跳转，包含开始/结束时间选择器、备注输入框，提交按钮。

\- \*\*个人中心\*\*：头像、姓名，下方菜单“我的使用记录”、“我的预约”、“设置”。



\---



\## 6. 非功能性需求



\- \*\*实时性\*\*：设备状态变更（开始/结束使用、预约成功）需实时更新所有用户界面，建议使用WebSocket或长轮询。

\- \*\*响应速度\*\*：列表加载时间 < 2秒，操作响应 < 1秒。

\- \*\*易用性\*\*：核心操作（登记、预约）最多3步完成，按钮文字明确。

\- \*\*数据准确性\*\*：时间冲突检测必须准确，避免并发问题（需在服务端加锁或事务）。

\- \*\*可扩展性\*\*：支持未来增加更多设备类型、功能模块。

\- \*\*安全性\*\*：用户需登录（可对接企业SSO或手机号验证），数据按用户隔离（普通员工仅看自己的记录，管理员看所有）。



\---



\## 7. 验收标准



\- \[ ] 管理员可成功添加一台新设备，并自动生成二维码。

\- \[ ] 员工扫描设备二维码可进入设备详情页。

\- \[ ] 员工登记使用，填写开始/结束时间，设备状态变为“使用中”。

\- \[ ] 员工结束使用，设备状态恢复“空闲”，记录准确。

\- \[ ] 员工预约未来时间段，系统能检测并阻止时间冲突。

\- \[ ] 员工可在预约开始前取消预约，释放时间。

\- \[ ] 设备详情页的日历/时间轴正确显示所有占用时段。

\- \[ ] 管理员查看统计看板，数据正确。

\- \[ ] APP在主流手机（iOS 14+、Android 10+）上运行流畅，无闪退。



\---



\## 8. 开发与部署建议



\- 推荐技术栈：React Native (跨平台) 或 Flutter，后端可用 Node.js + PostgreSQL，或使用 Supabase (BaaS) 简化开发。

\- 代码托管于 GitHub，使用 Codex 进行迭代开发，遵循“计划模式”。

\- 部署：iOS通过TestFlight内测，Android通过Google Play Internal Test，正式上线。



\---



\*本PRD为初稿，后续可根据实际开发情况进行调整和细化。\*

